# Context Export Is Independent And Optionally Importable

Context Export produces a self-contained Markdown document that operates independently from techies.tools during Generative UI work. AI-made preference changes return only through an optional Context Import: required Framework Definitions must validate, a structured diff must be reviewable, and no change reaches modular Framework sources without explicit approval; documents that drift from the contract remain standalone context but are not importable.
