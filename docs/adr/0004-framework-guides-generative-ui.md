# Framework Guides Generative UI

The Framework is broader than a design-token generator: it records personal design preferences as AI-readable guidance for composing consistent Generative UI. We begin with Primitives because higher-level guidance depends on them, then expand through semantic HTML elements, components, sections, and pages; representative websites remain validation Previews rather than the Framework's primary output.
