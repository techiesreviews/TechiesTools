---
interface Props {
  eyebrow: string;
  title: string;
  description: string;
}
const { eyebrow, title, description } = Astro.props;
---

<div class="guided-copy">
  <span class="guided-eyebrow">{eyebrow}</span>
  <h1 tabindex="-1">{title}</h1>
  <p>{description}</p>
</div>
